import numpy as np
#Part 1

arr_a = np.array([1,2,3, 4, 5, 6, 7, 8, 10])
'''
arr_b = np.array([2,3,4])
arr_c = np.vstack((arr_a, arr_b))
print(arr_c)

#Part 2
common_elements = np.intersect1d(arr_a, arr_b)
print(common_elements)

Part 3
print(arr_a[np.where(np.logical_and(arr_a > 5, arr_a < 10))])
'''

#Part 4
